---
interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<section class="rounded border border-black p-4">
  {title ? <h2 class="mb-2 text-xl font-semibold">{title}</h2> : null}
  <slot />
</section>
